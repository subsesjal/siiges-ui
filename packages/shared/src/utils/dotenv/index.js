import getConfigPath from './configPath';
import getEnvironmentVar from './getEnvironmentVar';
import setEnvironmentVar from './setEnvironmentVar';

const dotenv = {
  getConfigPath,
  getEnvironmentVar,
  setEnvironmentVar,
};

export {
  getConfigPath,
  getEnvironmentVar,
  setEnvironmentVar,
};

export default dotenv;
