import { Layout } from '@siiges-ui/shared';
import { FormRevalidaciones } from '@siiges-ui/revalidaciones';
import React from 'react';

export default function Revalidaciones() {
  return (
    <Layout title="Revalidaciones">
      <FormRevalidaciones />
    </Layout>
  );
}
